/**
 * Compatibility entry point: the router lazy-imports `@/features/chat/ChatPage`.
 * The page itself lives in ./page/ChatPage.tsx (see README.md).
 */
export { ChatPage, type ChatPageScope } from "./page/ChatPage";
